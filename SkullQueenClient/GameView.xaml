<UserControl x:Class="SkullQueenClient.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkullQueenClient"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="MainGrid">
        <Canvas x:Name="HandCanvas" Margin="20,0,80,0" Height="100" VerticalAlignment="Bottom"/>
        <Canvas x:Name="PlayersCanvas" Margin="0,0,0,300"/>
        <Canvas x:Name="PlayingFieldMiddle" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="100"/>
        <Canvas x:Name="PlayedCard" Margin="130,0,0,109" Height="96" Width="60" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        <Canvas x:Name="PlankCanvas"  Height="100" Width="70" Margin="24,0,0,135" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
        <TextBlock x:Name="StatusText" HorizontalAlignment="Left" Margin="10,0,0,249" TextWrapping="Wrap" Text="No Activity" VerticalAlignment="Bottom"/>
        <TextBlock x:Name="ScoreText" HorizontalAlignment="Right" Margin="0,177,20,0" TextWrapping="Wrap" Text="Score: 0" VerticalAlignment="Top"/>
        <Image x:Name="ProfilePictureImage" HorizontalAlignment="Right" Margin="0,250,0,0" VerticalAlignment="Top" Width="70"/>
        <Grid HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0, 0, 5, 5">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <RadioButton x:Name="RealRadioButton" Grid.Row="0" Content="Real" HorizontalAlignment="Left" VerticalAlignment="Top" GroupName="HandStyle" IsChecked="True" Checked="RadioButton_Checked"/>
            <RadioButton x:Name="ClassicRadioButton" Grid.Row="1" Content="Classic" HorizontalAlignment="Left" VerticalAlignment="Top" GroupName="HandStyle" Checked="RadioButton_Checked"/>
        </Grid>
        <Button x:Name="InfoButton" HorizontalAlignment="Left" Margin="749,204,0,0" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Click="InfoButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <TextBlock x:Name="InfoButtonContent" FontSize="24">🛈</TextBlock>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="Blue" TargetName="InfoButtonContent"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground" Value="RoyalBlue" TargetName="InfoButtonContent"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Popup
            x:Name="InfoPopup"
            PlacementTarget="{Binding ElementName=InfoButton}"
            Placement="Right"
            StaysOpen="False"
            >
            <Border BorderThickness="1" Background="White" BorderBrush="Black">
                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                   <ItemsControl x:Name="InfoItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="" TextWrapping="Wrap" Margin="5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</UserControl>
